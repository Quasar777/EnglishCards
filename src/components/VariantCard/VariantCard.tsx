import classes from "./VariantCard.module.scss";

const VariantCard = () => {
    return (
        <div className={classes.variantCard}>
            <p className={classes.variantCard__text}>translate</p>
        </div>
    );
}

export default VariantCard;
